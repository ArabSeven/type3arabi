[package]
name = "t3a-data"
description = "type3arabi.dat binary format: writer + zero-copy validated reader (docs/12)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# M2 adds: bytemuck (zero-copy section casts) and memmap2 behind feature "mmap" (ledger: docs/01 §8).
[dependencies]

[features]
mmap = []

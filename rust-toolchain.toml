[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
# Windows targets are added explicitly where needed (Windows CI / dev boxes):
#   rustup target add x86_64-pc-windows-msvc i686-pc-windows-msvc aarch64-pc-windows-msvc

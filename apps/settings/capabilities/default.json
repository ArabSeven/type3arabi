{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Settings window: core APIs only; config access goes through the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

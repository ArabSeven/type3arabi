# Settings app (M7)

Tauri 2 + vanilla TypeScript, bilingual (ar/en). Spec: `docs/05-ux-spec.md §7`, config schema: `docs/13-config-schema.md`,
decision: `docs/adr/0007-settings-app-with-tauri-2.md`. Scaffold with `cargo tauri init` in this folder during M7 and add
it to the workspace then (not before — it pulls a large dependency tree).
